<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Quattrocento:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <title>Home</title>

    <style>
      .dashboard-container {
        max-width: 400px;
        margin: 2rem auto;
        padding: 2rem;
        border: 1px solid #ccc;
        border-radius: 8px;
      }
      .form-group {
        margin-bottom: 1rem;
      }
      label {
        display: block;
        margin-bottom: 0.5rem;
      }
      input {
        width: 100%;
        padding: 0.5rem;
        margin-bottom: 1rem;
      }
      button {
        padding: 0.5rem 1rem;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
      }
      .safe-button {
        background-color: teal;
      }
      .warning-button {
        background-color: rgb(241, 87, 87);
        color: black;
      }
      .toggle-form {
        margin-top: 1rem;
        text-align: center;
      }
      .quattrocento-regular {
        font-family: "Quattrocento", serif;
        font-weight: 400;
        font-style: normal;
      }

      .quattrocento-bold {
        font-family: "Quattrocento", serif;
        font-weight: 700;
        font-style: normal;
      }
      .form-spacing {
        margin-bottom: 1rem;
      }
    </style>
  </head>
  <body>
    <%if(locals.user){%>
    <div class="dashboard-container quattrocento-regular">
      <h1 class="quattrocento-bold">Uploader Route</h1>

      <p>
        Welcome <%=user.name.charAt(0).toUpperCase() + user.name.slice(1) %>,
        you have logged in successfully!
      </p>
      <!-- <a href="/logout">Out</a> -->
      <form
        class="quattrocento-regular form-spacing"
        id="uploadForm"
        action="/auth/uploader"
        method="post"
      >
        <label for="file" class="logout quattrocento-regular"
          >Upload your file below</label
        >
        <input
          type="file"
          name="sampleFile"
          class="logout quattrocento-regular"
        />
        <button type="submit" class="logout quattrocento-regular safe-button">
          Upload
        </button>
      </form>

      <form action="/auth/logout" method="post" class="">
        <button
          type="submit"
          class="logout quattrocento-regular warning-button"
        >
          LOG OUT
        </button>
      </form>
    </div>
    <%} else{%>

    <div class="dashboard-container">
      <h3 class="quattrocento-bold">User not found</h3>
      <p class="quattrocento-regular">Please log in to proceed</p>
      <button
        class="quattrocento-regular"
        type="button"
        onclick="location.href='/auth'"
      >
        Log In Here
      </button>
    </div>
    <%}%>
  </body>
</html>
